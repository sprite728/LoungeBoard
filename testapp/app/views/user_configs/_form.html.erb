<%= form_for(@user_config) do |f| %>
  <% if @user_config.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_config.errors.count, "error") %> prohibited this user_config from being saved:</h2>

      <ul>
      <% @user_config.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
