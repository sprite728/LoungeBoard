<%= form_for(@bluetooth) do |f| %>
  <% if @bluetooth.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bluetooth.errors.count, "error") %> prohibited this bluetooth from being saved:</h2>

      <ul>
      <% @bluetooth.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :mac_addr %><br />
    <%= f.text_field :mac_addr %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
